server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        # 尝试访问实际文件，否则回退到 index.html
        try_files $uri /index.html;
    }

    error_page 404 /index.html;
}